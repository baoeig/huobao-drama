{
  "name": "huobao-drama-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "火宝短剧桌面版（Electron 壳）",
  "main": "dist/main.js",
  "scripts": {
    "build:backend": "node scripts/build-backend.mjs",
    "build:main": "node scripts/build-main.mjs",
    "rebuild:native": "electron-rebuild -f -w better-sqlite3",
    "dev": "npm run build:backend && npm run build:main && electron .",
    "dist": "npm run build:backend && npm run build:main && node scripts/prepare-resources.mjs && electron-builder --mac"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "electron": "^44.0.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  }
}
